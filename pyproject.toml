[project]
name = "strands-cookbook"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "aws-opentelemetry-distro>=0.12.0",
    "mcp>=1.13.0",
    "strands-agents-tools>=0.2.3",
    "strands-agents[a2a]>=1.34.0",
]
